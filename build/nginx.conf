#nginx/nginx.conf
events {
    worker_connections 1024;
}

http {
    server_tokens off;
    server {
        listen 80;
        listen [::]:80;
        server_name localhost;


        location / {
            proxy_pass http://192.168.0.101:8080/;
        }
    }
}
